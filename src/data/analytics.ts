// Approved Google Tag Manager container. GA4, Google Ads, Meta Pixel,
// and LinkedIn Insight Tag are NOT yet configured inside this
// container - that happens in a later phase, inside GTM's own
// interface, not in this codebase.
export const GTM_CONTAINER_ID = 'GTM-NVSFWQS';

// First-party localStorage key used to persist the visitor's consent
// choice (Basic Consent Mode). Shared between the early-restoration
// script in GtmHead and the ConsentBanner component so both read/write
// the exact same key.
export const CONSENT_STORAGE_KEY = 'mtm-consent';
